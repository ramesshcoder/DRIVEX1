SELECT
    c.Id,
    b.Name AS Brand,
    c.Model,
    cat.Name AS Category,
    c.Year,
    c.PricePerDay,
    c.Transmission,
    c.FuelType,
    c.Seats,
    c.IsAvailable
FROM Cars c
INNER JOIN Brands b
    ON c.BrandId = b.Id
INNER JOIN Categories cat
    ON c.CategoryId = cat.Id;